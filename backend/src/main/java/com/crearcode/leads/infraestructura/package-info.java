/**
 * Adaptadores del contexto {@code leads}: REST, persistencia JPA y
 * notificación por correo implementan los puertos definidos en
 * {@code dominio}. Es la única capa que puede depender de Spring y JPA.
 */
package com.crearcode.leads.infraestructura;

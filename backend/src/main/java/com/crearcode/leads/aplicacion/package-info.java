/**
 * Casos de uso del contexto {@code leads}: implementan los puertos de
 * entrada del dominio orquestando el propio dominio y sus puertos de
 * salida. Los métodos transaccionales viven aquí ({@code @Transactional}).
 */
package com.crearcode.leads.aplicacion;
